<div class="card">
    <div class="card-header">
        <h2><a href="/post/{{id}}">{{title}}</a></h2>
        <span>Posted by {{user.username}} on {{created_at}}</span>
            
    </div>
    <div class="card-body">
        {{post_text}}
    </div>
</div>