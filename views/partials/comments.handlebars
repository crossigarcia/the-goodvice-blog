{{!-- <div class="comments">
  {{#each this}}
    <section class="comment">
      <div class="meta">
        <div class="text">
          {{comment_text}}
        </div>
        {{user.username}}
        on
        {{format_date created_at}}
      </div>
      <div>
        {{#if loggedIn}}
          <button class="edit-btn" type="button">Edit comment</button>
          <button class="delete-comment-btn" type="submit">Delete comment</button>
        {{/if}}
      </div>
    </section>
  {{/each}}
</div> --}}

<div class=" comments container justify-content-center mt-5 border-left border-right">
  <div class="comment d-flex justify-content-center py-2">
    {{#each this}}
      <div class="second py-2 px-2"><span
          class="text1"
        >{{comment_text}}</span>
        <div class="d-flex justify-content-between py-1 pt-2">
          <div class="text2">{{user.username}}</div>
          <div><span class="text4">{{format_date created_at}}</span></div>
          <div>
            {{#if loggedIn}}
              <button class="edit-btn" type="button">Edit comment</button>
              <button class="delete-comment-btn" type="submit">Delete comment</button>
            {{/if}}
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>

<script src="/javascript/delete-comment.js"></script>
<script src="/javascript/edit-comment.js"></script>